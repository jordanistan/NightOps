# Export adapters

Export adapters project canonical application records into external knowledge
systems. The current adapter is [`obsidian/`](obsidian/), which writes atomic
Markdown notes, indexes, links, and generated mission sections into the
configured vault.

Exports must preserve user-authored notes and flight-recorder content, retain
source attribution, and label unavailable provider data honestly.
